# Veri kaynağı: mock (dahili, offline kiosk) | live (harici ERP gateway)
VITE_DATA_SOURCE=mock

# live modda gateway uçları (npm run gateway ile başlat):
# VITE_DATA_SOURCE=live
# VITE_API_URL=http://localhost:8787
# VITE_WS_URL=ws://localhost:8787/ws
