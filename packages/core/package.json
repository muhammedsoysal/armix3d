{
  "name": "@metalnest/core",
  "version": "0.1.0",
  "private": true,
  "description": "UI'dan bağımsız paylaşılan çekirdek: veri modelleri, provider arayüzleri, karar motoru, state machine",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zustand": "^4.5.5"
  },
  "devDependencies": {
    "typescript": "~5.6.3",
    "vitest": "^4.1.10"
  }
}
