{
  "name": "@metalnest/electron-shell",
  "version": "0.1.0",
  "private": true,
  "description": "Electron ana süreç — mod seçim ekranı ve kiosk kabuğu",
  "main": "dist/main.cjs",
  "scripts": {
    "build:main": "esbuild main.ts --bundle --platform=node --format=cjs --external:electron --outfile=dist/main.cjs",
    "dev": "wait-on http-get://localhost:5173 && npm run build:main && electron .",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "electron": "^33.0.0",
    "esbuild": "^0.24.0",
    "typescript": "~5.6.3",
    "wait-on": "^8.0.1"
  }
}
